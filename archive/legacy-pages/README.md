# legacy pages

dead-code archive for the original portfolio pages that used to live at `/`
and `/projects` before the geocities-style redesign became the homepage.

these files are kept for reference only. they are intentionally outside
`pages/` so next.js will not route or render them.
